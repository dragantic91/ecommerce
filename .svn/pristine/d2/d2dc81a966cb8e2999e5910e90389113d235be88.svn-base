<?php
$image = $product->image;
$imageType = (isset($imageType)) ? $imageType : "smallUrl"
?>
@if(NULL !== $image)
    <img alt="{{ $product->title }}"
         src="{{ $image->$imageType }}"/>
@endif